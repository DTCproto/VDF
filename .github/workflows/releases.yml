name: BUILD_BY_RELEASES(VDF)

on:
  # you may only define up to 10 `inputs` for a `workflow_dispatch` event
  workflow_dispatch:
    inputs:
      vdf_commit_id:
        description: "指定VDF使用commit id版本"
        required: true
        default: "HEAD~0"
        type: string
      release_archives:
        description: "enable release archives"
        default: true
        type: boolean

jobs:
  pre:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
      attestations: write
      id-token: write
    outputs:
      vdf_sha_short: ${{ steps.set_global_params.outputs.vdf_sha_short }}
    steps:
      - name: set global params
        id: set_global_params
        shell: bash
        run: |
          mkdir -p ${{ github.workspace }}/build
          cd ${{ github.workspace }}/build
          git clone -b master https://github.com/0x90d/videoduplicatefinder.git
          cd ${{ github.workspace }}/build/videoduplicatefinder
          git checkout --force --quiet ${{ inputs.vdf_commit_id }}
          git submodule update --init --recursive
          echo "vdf_sha_short=$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"

  build:
    needs: [pre]
    permissions:
      contents: write
      packages: write
      attestations: write
      id-token: write

    strategy:
      matrix:
        kind: ['linux', 'windows', 'linuxarm64']
        include:
          - kind: linux
            os: ubuntu-latest
            target: linux-x64
            name: linux-x64
          - kind: windows
            os: windows-latest
            target: win-x64
            name: win-x64
          - kind: linuxarm64
            # Native ARM64 runner: Native AOT cannot cross-compile from x64 to
            # linux-arm64 without a cross toolchain, so build it on its own arch.
            os: ubuntu-24.04-arm
            target: linux-arm64
            name: linux-arm64

    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7
        with:
          submodules: 'recursive'
          fetch-depth: 0

      - name: git clone VDF
        shell: bash
        run: |
          cd $GITHUB_WORKSPACE/app/src/videoduplicatefinder/
          git checkout --force --quiet ${{ needs.pre.outputs.vdf_sha_short }}
          git submodule update --init --recursive

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'

      - name: change language (Linux)
        if: matrix.kind != 'windows'
        shell: bash
        run: |
          if [ "$(ls -A "$GITHUB_WORKSPACE/replace" 2>/dev/null)" ]; then
            cp -rf "$GITHUB_WORKSPACE/replace/"* "$GITHUB_WORKSPACE/app/src/videoduplicatefinder/"
          fi

      - name: change language (Windows)
        if: matrix.kind == 'windows'
        shell: powershell
        run: |
          if ((Get-ChildItem "$env:GITHUB_WORKSPACE\replace" -ErrorAction SilentlyContinue).Count -gt 0) {
            Copy-Item "$env:GITHUB_WORKSPACE\replace\*" "$env:GITHUB_WORKSPACE\app\src\videoduplicatefinder" -Recurse -Force
          }

      - name: build and zip
        shell: bash
        run: |
            cd $GITHUB_WORKSPACE/app/src/videoduplicatefinder/
            release_name="App-${{ matrix.target }}-4.0.${{ needs.pre.outputs.vdf_sha_short }}"
            # Build everything
            # Build GUI (Native AOT — verified AOT-safe for 4.0: compiled bindings,
            # source-generated JSON, statically-registered MemoryPack formatters)
            dotnet publish ./VDF.GUI/VDF.GUI.csproj -c Release -v q -p:PublishAot=true -r "${{ matrix.target }}" -o "outputGUI"
            # Build CLI (Native AOT)
            dotnet publish ./VDF.CLI/VDF.CLI.csproj -c Release -v q -p:PublishAot=true -r "${{ matrix.target }}" -o "outputCLI"
            # Build Web (JIT, self-contained — Blazor Server is not supported under Native AOT)
            dotnet publish ./VDF.Web/VDF.Web.csproj -c Release -v q --self-contained -r "${{ matrix.target }}" -o "outputWeb"
          
            # Pack files
            if [ "${{ matrix.target }}" == "win-x64" ]; then
              # Pack to zip for Windows
              7z a -tzip "GUI-${release_name}.zip" "./outputGUI/*"
              7z a -tzip "CLI-${release_name}.zip" "./outputCLI/*"
              7z a -tzip "Web-${release_name}.zip" "./outputWeb/*"
            else
              tar czvf "GUI-${release_name}.tar.gz" "outputGUI"
              tar czvf "CLI-${release_name}.tar.gz" "outputCLI"
              tar czvf "Web-${release_name}.tar.gz" "outputWeb"
            fi
            # Clean up
            rm -rf "outputGUI" "outputCLI" "outputWeb" "Video Duplicate Finder.app"

      - name: Upload zips
        uses: actions/upload-artifact@v7
        with:
          name: bin-App-${{ matrix.target }}-3.0.${{ needs.pre.outputs.vdf_sha_short }}
          compression-level: 9
          path: |
            ${{ github.workspace }}/app/src/videoduplicatefinder/GUI-App-*
            ${{ github.workspace }}/app/src/videoduplicatefinder/CLI-App-*
            ${{ github.workspace }}/app/src/videoduplicatefinder/Web-App-*

  release:
    if: ${{ inputs.release_archives }}
    needs: [pre, build]
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
      attestations: write
      id-token: write
    steps:
      - name: Download zips archives
        uses: actions/download-artifact@v8
        with:
          pattern: "bin*"
          merge-multiple: true
          path: ${{ github.workspace }}/archives

      - name: show zips files
        run: |
          tree ${{ github.workspace }}/archives

      - name: Release archives
        uses: softprops/action-gh-release@v3
        with:
          tag_name: "4.0.${{ needs.pre.outputs.vdf_sha_short }}"
          name: "4.0.${{ needs.pre.outputs.vdf_sha_short }}"
          token: ${{ secrets.GITHUB_TOKEN }}
          prerelease: false
          body: |
            [Last build based on ${{ needs.pre.outputs.vdf_sha_short }}]
            Note: 
            [Attachments are automatically created and replaced on every new commit.]
            Regardless of what the message above says. 
            If you experience upgrade issues you may need to delete your 'ScannedFiles.db' file in VDF folder.
          files: |
            ${{ github.workspace }}/archives/**
